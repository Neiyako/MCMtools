template_id: tab.validation_metrics
kind: table
version: 1.0.0
purpose: Report the full validation battery for the chosen model.
observed_in: required whenever a model is fitted; reviewers look for it
evidence:
- '2023 C/2307166 Table 2: R2, RMSE, and residual summary'
- '2022 C/2200401 Table 4: in-sample vs out-of-sample metrics'
requires:
  model: true
  dataset: false
  required_inputs:
  - bindings
inputs:
- name: bindings
  type: array
  role: result_atoms
  description: Cell bindings to ResultAtoms
outputs:
- name: table
  type: file
  role: artifact
  description: LaTeX table
- name: table_record
  type: any
  role: metadata
  description: A populated Table record
defaults:
  columns:
  - Metric
  - Value
  - Definition
  caption_template: Validation metrics for the fitted model.
purpose_cn: 所选模型的完整验证指标。
