template_id: tab.metric_comparison
kind: table
version: 1.0.0
purpose: Models x metrics comparison table, bound cell-by-cell to ResultAtoms.
observed_in: the standard vehicle for the model-selection claim
evidence:
- '2023 C/2310767 Table 3: Lasso 2.3771/1.5417/1.0727/0.6847 ... Stacking 1.6768/1.2949/0.8623/0.8377'
requires:
  model: true
  dataset: false
  required_inputs:
  - bindings
inputs:
- name: bindings
  type: array
  role: result_atoms
  description: Cell bindings to ResultAtoms
outputs:
- name: table
  type: file
  role: artifact
  description: LaTeX table
- name: table_record
  type: any
  role: metadata
  description: A populated Table record
defaults:
  columns:
  - Model
  - MSE
  - RMSE
  - MAE
  - R2
caption_template: Cross-validation performance of candidate models.
purpose_cn: 模型×指标对比表，逐格绑定到结果原子，数字不会对不上。
